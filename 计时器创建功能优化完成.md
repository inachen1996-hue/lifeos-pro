# 计时器创建功能优化完成

## 修改内容

### 1. 移除图标选择功能
- **之前**：用户需要从12个图标中手动选择一个
- **现在**：根据计时器模式自动分配图标
  - 秒表模式：⏱️
  - 倒计时模式：⏰
  - 番茄钟模式：🍅

### 2. 优化时长设置界面
- **倒计时模式**：
  - 标签改为"倒计时长（分钟）"
  - 添加快捷按钮：5、10、15、25、30、45、60分钟
  - 输入框样式优化，更大更清晰

- **番茄钟模式**：
  - 标签改为"忙多久（分钟）"和"休息多久（分钟）"
  - 工作时长快捷按钮：15、25、30、45分钟
  - 休息时长快捷按钮：5、10、15、20分钟
  - 移除了"循环次数"设置（简化界面）

### 3. 创建后立即开始功能
- **新增确认对话框**：
  - 创建计时器后自动弹出确认对话框
  - 显示创建的计时器名称和图标
  - 提供两个选项：
    - "稍后开始"：关闭对话框，不启动计时器
    - "立即开始"：直接启动计时器并开始计时

### 4. 界面优化
- 移除了图标选择区域，界面更简洁
- 时长输入框增大，文字居中，更易操作
- 快捷按钮采用不同颜色区分（倒计时橙色、工作红色、休息绿色）
- 确认对话框采用绿色主题，传达积极的创建成功信息

## 用户体验改进

1. **更快的创建流程**：减少了图标选择步骤
2. **更直观的设置**：快捷按钮让常用时长一键设置
3. **更流畅的使用**：创建后可立即开始，无需再次点击
4. **更清晰的界面**：标签文字更口语化（"忙多久"、"休息多久"）

## 技术实现

- 添加了 `getIconForMode()` 函数自动选择图标
- 添加了 `showStartConfirm` 状态控制确认对话框
- 添加了 `createdTimer` 状态保存创建的计时器
- 添加了 `handleStartNow()` 和 `handleNotStart()` 处理用户选择
- 传递 `onStartTimer` 回调函数到 TimerCreateModal 组件

## 测试建议

1. 创建秒表模式计时器，验证图标是否为 ⏱️
2. 创建倒计时模式，测试快捷按钮和自定义时长
3. 创建番茄钟模式，测试工作和休息时长设置
4. 点击"立即开始"，验证计时器是否正确启动
5. 点击"稍后开始"，验证计时器是否已创建但未启动
