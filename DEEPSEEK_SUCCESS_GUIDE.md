# ✅ DeepSeek API 成功使用指南

## 当前状态
- ✅ 本地服务器已启动（端口 8001）
- ✅ 应用可访问：http://localhost:8001/index.html
- ⏳ 需要测试 API Key 是否有效

## 📋 完整测试步骤

### 步骤 1：打开测试页面
1. 确保服务器正在运行（终端窗口不要关闭）
2. 在浏览器中打开：**http://localhost:8001/test-deepseek-browser.html**
3. 如果页面打不开，检查终端是否显示 "Available on: http://127.0.0.1:8001"

### 步骤 2：测试 API Key
1. 在测试页面输入你的 DeepSeek API Key（以 `sk-` 开头）
2. 点击 "🚀 测试连接" 按钮
3. 等待 3-5 秒

### 步骤 3：查看结果

#### ✅ 如果成功：
- 会显示绿色框
- 显示 "✅ 测试成功！"
- 显示 AI 的回复内容
- **继续到步骤 4**

#### ❌ 如果失败：
会显示红色框，根据错误信息处理：

**错误 1：401 Unauthorized**
```
原因：API Key 无效或未激活
解决：
1. 访问 https://platform.deepseek.com/api_keys
2. 检查 API Key 是否正确复制（包括 sk- 前缀）
3. 确认账户已充值（至少 1 元）
4. 尝试重新生成一个新的 API Key
```

**错误 2：Failed to fetch**
```
原因：网络连接问题或 CORS
解决：
1. 检查网络连接
2. 确认是通过 http://localhost:8001 访问（不是 file://）
3. 尝试关闭 VPN 或代理
4. 检查防火墙设置
```

**错误 3：429 Too Many Requests**
```
原因：请求过于频繁
解决：等待 10 秒后重试
```

### 步骤 4：在主应用中使用
如果测试页面成功，现在配置主应用：

1. 打开：**http://localhost:8001/index.html**
2. 点击右上角 "⚙️ 设置"
3. 在 "AI 服务提供商" 选择 "DeepSeek"
4. 输入你的 API Key（和测试页面用的同一个）
5. 点击 "🧪 测试连接"
6. 应该显示 "✅ DEEPSEEK API 连接成功！"
7. 点击 "保存设置"

### 步骤 5：开始使用
1. 在主页面创建一些计划事项
2. 点击 "复盘" 按钮
3. 选择 "AI 生成总结"
4. 等待 AI 生成内容

## 🔍 调试技巧

### 查看浏览器控制台
如果遇到问题，按 F12 打开开发者工具：
1. 点击 "Console"（控制台）标签
2. 查看红色错误信息
3. 截图发给我，我可以帮你分析

### 检查 API Key 格式
正确的格式：
```
sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```
- 必须以 `sk-` 开头
- 后面是一串字母和数字
- 总长度约 40-50 个字符

### 确认账户余额
1. 访问：https://platform.deepseek.com/usage
2. 检查 "可用余额" 是否大于 0
3. 如果余额为 0，需要充值（最低 1 元）

## 💡 常见问题

**Q: 为什么要用 http://localhost:8001 而不是直接打开文件？**
A: 因为浏览器的安全策略（CORS）不允许本地文件直接访问外部 API。通过本地服务器可以绕过这个限制。

**Q: 测试页面成功了，但主应用还是失败？**
A: 
1. 确认主应用也是通过 http://localhost:8001/index.html 访问
2. 检查是否输入了相同的 API Key
3. 按 F12 查看控制台错误信息

**Q: 服务器关闭后怎么重新启动？**
A: 
1. 打开终端（Terminal）
2. 输入：`cd /Users/你的用户名/项目路径/lifeos-pro`
3. 输入：`npx http-server -p 8001`
4. 看到 "Available on" 就成功了

**Q: 如何找到项目路径？**
A: 在 Kiro IDE 中，项目路径通常显示在左侧文件树的顶部

## 📞 需要帮助？

如果按照以上步骤还是不成功，请告诉我：
1. 在哪一步失败了？
2. 看到什么错误信息？
3. 浏览器控制台（F12）显示什么？
4. 截图会更有帮助！

我会帮你一步步解决问题。
