# 📁 新建分类功能

## ✨ 功能说明

将顶部的"新建"按钮改为"新建分类"按钮，支持创建自定义分类，并自动同步到数据源的习惯管理中。

## 🎯 功能特性

### 1. 新建分类按钮
- **位置**：左侧栏顶部
- **样式**：紫色渐变背景（区别于原来的深灰色）
- **功能**：点击弹出创建分类模态框

### 2. 创建分类模态框
- **分类名称**：
  - 文本输入框
  - 建议 2-3 个字
  - 最多 4 个字
  - 必填项

- **分类颜色**：
  - 8 种 Macaron 配色可选
  - 蓝色、绿色、粉色、紫色、黄色、橙色、玫瑰、翠绿
  - 可视化颜色预览

### 3. 数据同步
- ✅ 保存到 CategoryStorage
- ✅ 自动更新分类列表
- ✅ 显示创建成功提示

## 📱 界面设计

### 新建分类按钮
```
┌────┐
│ +  │ ← 紫色渐变背景
└────┘
点击创建新分类
```

### 创建分类模态框
```
┌─────────────────────────────────────┐
│ 创建新分类                      [×]  │
├─────────────────────────────────────┤
│ 分类名称                             │
│ [运动]                               │
│ 建议 2-3 个字，最多 4 个字           │
│                                      │
│ 分类颜色                             │
│ ┌───┬───┬───┬───┐                   │
│ │蓝 │绿 │粉 │紫 │                   │
│ ├───┼───┼───┼───┤                   │
│ │黄 │橙 │玫 │翠 │                   │
│ └───┴───┴───┴───┘                   │
│                                      │
│ [创建分类]                           │
└─────────────────────────────────────┘
```

## 🎨 视觉效果

### 按钮颜色变化
```
原来（新建计时器）：
深灰色渐变
from-slate-700 to-slate-900

现在（新建分类）：
紫色渐变
from-purple-600 to-purple-800
```

### 颜色选择器
```
┌────────────────────────────────┐
│ 蓝色  绿色  粉色  紫色          │
│ [■]   [■]   [■]   [■]          │
│                                 │
│ 黄色  橙色  玫瑰  翠绿          │
│ [■]   [■]   [■]   [■]          │
└────────────────────────────────┘
选中的颜色有紫色边框和放大效果
```

## 🔄 使用流程

### 创建新分类
1. 点击左侧栏顶部的 + 按钮（紫色）
2. 输入分类名称（如"运动"）
3. 选择分类颜色（如蓝色）
4. 点击"创建分类"按钮
5. 分类创建成功，显示在分类列表中

### 使用新分类
1. 创建分类后，自动出现在左侧分类列表
2. 点击新分类，可以查看该分类下的计时器
3. 创建计时器时，可以选择新分类

## 🔧 技术实现

### 新增组件
```jsx
const CategoryCreateModal = ({ onClose, onCreate }) => {
  const [name, setName] = useState('');
  const [color, setColor] = useState('bg-macaron-blue');
  
  const handleSubmit = () => {
    const category = {
      id: `category_${Date.now()}`,
      name: name.trim(),
      color,
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString()
    };
    onCreate(category);
    onClose();
  };
  
  // ... 渲染逻辑
};
```

### 新增函数
```jsx
const handleCreateCategory = (category) => {
  if (window.TimerBackend) {
    // 保存到 CategoryStorage
    window.TimerBackend.CategoryStorage.saveCategory(category);
    setTimerCategories(window.TimerBackend.CategoryStorage.loadCategories());
    showToast('分类已创建');
  }
};
```

### 状态管理
```jsx
const [showCreateCategoryModal, setShowCreateCategoryModal] = useState(false);
```

## 📊 数据结构

### 分类对象
```javascript
{
  id: "category_1733654321000",      // 唯一 ID
  name: "运动",                       // 分类名称
  color: "bg-macaron-blue",          // 分类颜色
  createdAt: "2025-12-08T10:00:00Z", // 创建时间
  updatedAt: "2025-12-08T10:00:00Z"  // 更新时间
}
```

### 存储位置
- **CategoryStorage**：`window.TimerBackend.CategoryStorage`
- **LocalStorage Key**：由 CategoryStorage 管理

## 🎯 与数据源的同步

### 当前实现
- ✅ 分类保存到 CategoryStorage
- ✅ 分类列表自动更新
- ✅ 创建计时器时可选择新分类

### 未来扩展（需要额外实现）
如果需要与"数据源-习惯管理"完全同步，需要：
1. 在创建分类时，同时更新 CATEGORIES 常量
2. 或者让习惯管理读取 CategoryStorage
3. 确保两边的分类列表保持一致

## ✅ 验证步骤

1. **刷新浏览器**
   - Mac: `Cmd + Shift + R`
   - Windows/Linux: `Ctrl + Shift + R`

2. **检查新建按钮**
   - 左侧栏顶部的 + 按钮是紫色渐变
   - 鼠标悬停显示"创建新分类"

3. **测试创建分类**
   - 点击 + 按钮
   - 弹出"创建新分类"模态框
   - 输入名称（如"运动"）
   - 选择颜色（如蓝色）
   - 点击"创建分类"

4. **验证分类创建**
   - 模态框关闭
   - 显示"分类已创建"提示
   - 新分类出现在分类列表中
   - 可以点击选中新分类

5. **测试使用新分类**
   - 点击右侧的"创建新任务"按钮
   - 在分类下拉框中可以看到新分类
   - 可以为新分类创建计时器

## 🎉 完成标志

当你看到以下效果时，说明功能正常：

✅ 顶部按钮是紫色渐变  
✅ 点击弹出"创建新分类"模态框  
✅ 可以输入分类名称  
✅ 可以选择分类颜色  
✅ 点击创建后分类出现在列表中  
✅ 新分类可以正常使用  
✅ 显示创建成功提示  

## 🚧 注意事项

### 分类名称限制
- 建议 2-3 个字（如"工作"、"学习"）
- 最多 4 个字（如"深度工作"）
- 超过 4 个字可能显示不完整

### 颜色选择
- 提供 8 种 Macaron 配色
- 建议选择与现有分类不同的颜色
- 便于视觉区分

### 数据持久化
- 分类数据保存在 LocalStorage
- 刷新页面后仍然存在
- 可以通过 CategoryStorage 管理

---

**功能完成时间**: 2025-12-08  
**状态**: ✅ 完成  
**需要操作**: 刷新浏览器验证
