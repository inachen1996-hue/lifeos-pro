# 番茄钟功能统一完成 🍅

## 统一概述
确保计时器tab中的番茄钟功能与今日计划中的番茄钟功能保持完全一致，提供统一的用户体验。

## 统一的功能特性 ✨

### 1. 循环次数设置 🔄
**所有入口都支持：**
- 📍 计时器tab → 创建计时器 → 番茄钟
- 📍 计时器tab → 启动已有计时器 → 选择番茄钟模式
- 📍 今日计划 → 计划事项 → 启动番茄钟

**统一配置：**
- 默认值：4轮
- 可选范围：1-10轮
- 快捷选择：2轮、4轮、6轮、8轮

### 2. 时长设置 ⏰
**工作时长：**
- 默认：25分钟
- 范围：1-90分钟
- 快捷选择：15、25、30、45分钟

**休息时长：**
- 默认：5分钟
- 范围：1-30分钟
- 快捷选择：5、10、15、20分钟

### 3. 周期完成确认 ✅
**工作完成时：**
- 🕐 选项1：继续工作（正计时延长）
- ☕ 选项2：开始休息（标准流程）

**休息完成时：**
- 🕐 选项1：继续休息（正计时延长）
- 🍅 选项2：开始工作（进入下一轮）

### 4. 延长时间功能 ⏱️
**延长显示：**
- 时间格式：`+00:05:30`（黄色）
- 状态提示：`（延长中）`
- 时长说明：`25 分钟 + 延长时间`

**数据记录：**
- ✅ 工作延长时间：记录
- ❌ 休息延长时间：不记录

### 5. 循环进度显示 📊
**实时反馈：**
- 当前周期：🍅 工作时间 / ☕ 休息时间
- 循环进度：第 X / Y 轮
- 进度徽章：紫色马卡龙样式

## 统一的UI设计 🎨

### 设置界面
```
┌─────────────────────────────────┐
│  设置时长                       │
│  📋 任务名称 / 🍅 计时器名称   │
│                                 │
│  工作时长（分钟）               │
│  [    25    ]                   │
│  [15] [25] [30] [45]           │
│                                 │
│  休息时长（分钟）               │
│  [     5    ]                   │
│  [5] [10] [15] [20]            │
│                                 │
│  循环次数                       │
│  [     4    ]                   │
│  [2轮] [4轮] [6轮] [8轮]       │
│                                 │
│  [  取消  ] [  开始计时  ]     │
└─────────────────────────────────┘
```

### 运行界面
```
┌─────────────────────────────────┐
│  🍅 深度工作                    │
│                                 │
│  🎵 时间到！                    │
│  [  停止响铃  ]                │
│                                 │
│       00:25:00                  │
│                                 │
│  🍅 工作时间                    │
│  25 分钟                        │
│  第 2 / 4 轮                    │
│                                 │
│  选择下一步操作：               │
│  ┌──────────┐  ┌──────────┐   │
│  │  🕐      │  │  ☕      │   │
│  │继续工作  │  │开始休息  │   │
│  │ 正计时   │  │ 5分钟    │   │
│  └──────────┘  └──────────┘   │
│                                 │
│  [  结束计时  ]                │
└─────────────────────────────────┘
```

### 延长时间界面
```
┌─────────────────────────────────┐
│  🍅 深度工作                    │
│                                 │
│      +00:05:30                  │
│     （黄色显示）                │
│                                 │
│  🍅 工作时间 （延长中）         │
│  25 分钟 + 延长时间             │
│  第 2 / 4 轮                    │
│                                 │
│  [  暂停  ] [  停止  ]         │
└─────────────────────────────────┘
```

## 统一的交互流程 🔄

### 流程1：从计时器tab启动
```
1. 进入计时器tab
2. 点击已有计时器的启动按钮
3. 选择"番茄钟"模式
4. 设置工作时长、休息时长、循环次数
5. 点击"开始计时"
6. 进入番茄钟运行界面
```

### 流程2：从今日计划启动
```
1. 进入今日计划
2. 点击计划事项的计时按钮
3. 选择"番茄钟"模式
4. 设置工作时长、休息时长、循环次数
5. 点击"开始计时"
6. 进入番茄钟运行界面
```

### 流程3：工作周期完成
```
1. 工作25分钟 → 响铃
2. 点击"停止响铃"
3. 看到两个选项：
   - 继续工作（正计时）
   - 开始休息（5分钟）
4. 选择后进入对应状态
```

### 流程4：休息周期完成
```
1. 休息5分钟 → 响铃
2. 点击"停止响铃"
3. 看到两个选项：
   - 继续休息（正计时）
   - 开始工作（进入下一轮）
4. 选择后进入对应状态
```

## 统一的配置项 ⚙️

### TimerTypeSelectModal（计时器tab）
```javascript
const [workDuration, setWorkDuration] = useState(timer.settings?.workDuration || 25);
const [restDuration, setRestDuration] = useState(timer.settings?.restDuration || 5);
const [cycles, setCycles] = useState(timer.settings?.cycles || 4);
```

### PlanItemTimerModal（今日计划）
```javascript
const [workDuration, setWorkDuration] = useState(25);
const [restDuration, setRestDuration] = useState(5);
const [cycles, setCycles] = useState(4);
```

### TimerCreateModal（创建计时器）
```javascript
const [workDuration, setWorkDuration] = useState(25);
const [restDuration, setRestDuration] = useState(5);
const [cycles, setCycles] = useState(4);
```

## 统一的数据结构 📦

### Timer Settings
```typescript
interface TimerSettings {
  // 番茄钟模式
  workDuration?: number;    // 工作时长（分钟）
  restDuration?: number;    // 休息时长（分钟）
  cycles?: number;          // 循环次数
}
```

### Timer Session
```typescript
interface TimerSession {
  timerId: string;
  startTime: string;
  mode: 'pomodoro';
  status: 'running' | 'paused';
  currentCycle?: number;     // 当前循环
  currentPeriod?: 'work' | 'rest';  // 当前周期
  workPeriods?: Array<{      // 工作周期记录
    start: string;
    end: string;
  }>;
}
```

## 统一的视觉样式 🎨

### 颜色方案
- 工作时间：🍅 玫瑰色 `bg-macaron-rose`
- 休息时间：☕ 绿色 `bg-macaron-green`
- 循环进度：🔄 紫色 `bg-macaron-purple`
- 延长时间：⏱️ 黄色 `text-yellow-600`
- 响铃状态：🔔 红色 `text-rose-600`

### 按钮样式
- 继续当前：黄色 `bg-macaron-yellow`
- 切换状态：绿色 `bg-macaron-green`
- 暂停：黄色 `bg-macaron-yellow`
- 停止：玫瑰色 `bg-macaron-rose`

### 徽章样式
- 循环进度：`bg-macaron-purple/20` + `text-macaron-purple`
- 延长提示：`text-macaron-yellow`

## 修改的文件 📝

### 前端UI
- ✅ `index.html` - TimerTypeSelectModal 添加循环次数
- ✅ `index.html` - PlanItemTimerModal 已有循环次数
- ✅ `index.html` - TimerCreateModal 已有循环次数
- ✅ `index.html` - TimerControlModal 统一运行界面

### 后端引擎
- ✅ `src/pomodoro-engine.ts` - 统一的番茄钟逻辑

## 功能对比表 📊

| 功能 | 计时器tab | 今日计划 | 状态 |
|------|-----------|----------|------|
| 循环次数设置 | ✅ | ✅ | 统一 |
| 工作时长设置 | ✅ | ✅ | 统一 |
| 休息时长设置 | ✅ | ✅ | 统一 |
| 周期完成确认 | ✅ | ✅ | 统一 |
| 延长时间功能 | ✅ | ✅ | 统一 |
| 循环进度显示 | ✅ | ✅ | 统一 |
| 响铃提醒 | ✅ | ✅ | 统一 |
| 数据记录 | ✅ | ✅ | 统一 |

## 测试检查清单 ✅

### 功能测试
- [ ] 从计时器tab启动番茄钟
- [ ] 从今日计划启动番茄钟
- [ ] 设置不同的循环次数
- [ ] 测试工作周期完成确认
- [ ] 测试休息周期完成确认
- [ ] 测试延长时间功能
- [ ] 测试循环进度显示
- [ ] 测试数据记录

### UI测试
- [ ] 检查设置界面一致性
- [ ] 检查运行界面一致性
- [ ] 检查按钮样式一致性
- [ ] 检查颜色方案一致性
- [ ] 检查文字提示一致性

### 交互测试
- [ ] 检查操作流程一致性
- [ ] 检查响应速度一致性
- [ ] 检查动画效果一致性

## 版本信息
- 统一日期：2024-12-09
- 版本：v3.9.5
- 范围：番茄钟功能全面统一

---

**统一完成！** 🎉 计时器tab和今日计划中的番茄钟功能现在完全一致！
