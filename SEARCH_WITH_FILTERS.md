# 搜索与筛选组合功能

## 功能描述
搜索后可以使用日期和时间段进行二次筛选，提供更灵活的数据查找方式。

## 功能特性

### 1. 搜索时自动清空筛选
当用户开始输入搜索关键词时：
- ✅ 自动清空日期选择
- ✅ 自动设置时间段为"全部"
- ✅ 展示所有匹配的搜索结果

### 2. 搜索后可二次筛选
搜索结果出来后，用户可以：
- ✅ 选择日期，筛选特定日期的搜索结果
- ✅ 选择时间段，筛选特定时间段的搜索结果
- ✅ 组合使用日期和时间段筛选

### 3. 日期选择增强
- ✅ 日期可以为空（不筛选）
- ✅ 添加清空按钮（X 图标）
- ✅ 点击清空按钮，显示所有数据

## 使用流程

### 流程 1：搜索 → 查看所有结果
1. 输入"工作"
2. 自动清空日期和时间段
3. 显示所有日期的"工作"记录

### 流程 2：搜索 → 按日期筛选
1. 输入"工作"
2. 显示所有"工作"记录
3. 选择日期"2024-12-08"
4. 只显示 12月8日 的"工作"记录

### 流程 3：搜索 → 按时间段筛选
1. 输入"学习"
2. 显示所有"学习"记录
3. 点击"下午"
4. 只显示下午的"学习"记录

### 流程 4：搜索 → 组合筛选
1. 输入"工作"
2. 选择日期"2024-12-08"
3. 点击"下午"
4. 只显示 12月8日下午 的"工作"记录

### 流程 5：清空日期
1. 点击日期输入框右侧的 X 按钮
2. 清空日期选择
3. 显示所有日期的数据

## 技术实现

### 1. 自动清空筛选
```javascript
useEffect(() => {
  if (searchQuery.trim()) {
    setSelectedDate('');      // 清空日期
    setTimePeriod('all');     // 设置为"全部"
  }
}, [searchQuery]);
```

### 2. 组合筛选逻辑
```javascript
const filteredData = useMemo(() => {
  let data = parseHistoryData();

  // 1. 按名称搜索
  if (searchQuery.trim()) {
    data = data.filter(item => /* 名称匹配 */);
  }

  // 2. 按日期筛选（只有在有日期选择时）
  if (selectedDate) {
    data = data.filter(item => item.date === selectedDate);
  }

  // 3. 按时间段筛选
  if (timePeriod !== 'all') {
    data = data.filter(item => /* 时间段匹配 */);
  }

  return data;
}, [searchQuery, selectedDate, timePeriod]);
```

### 3. 日期清空按钮
```javascript
{selectedDate && (
  <button
    onClick={() => setSelectedDate('')}
    className="absolute right-2 top-1/2 -translate-y-1/2 p-1 bg-slate-200 rounded-full"
  >
    <X className="w-3 h-3" />
  </button>
)}
```

## 用户体验

### 搜索模式
**输入搜索关键词时**：
- 日期和时间段自动清空
- 显示所有匹配结果
- 可以手动选择日期或时间段进行二次筛选

### 筛选模式
**没有搜索关键词时**：
- 默认显示今天的数据
- 默认显示当前时间段的数据
- 可以切换日期和时间段

### 清空操作
**清空日期**：
- 点击日期输入框右侧的 X 按钮
- 显示所有日期的数据

**清空搜索**：
- 清空搜索框
- 恢复默认的日期和时间段筛选

## 界面示例

### 搜索状态
```
┌─────────────────────────────────────┐
│ 🔍 工作                              │
├─────────────────────────────────────┤
│ 📅 [空]                         [X]  │
├─────────────────────────────────────┤
│ [全部] [凌晨] [早上] [下午] [晚上]   │
└─────────────────────────────────────┘

结果：显示所有日期、所有时间段的"工作"记录
```

### 搜索 + 日期筛选
```
┌─────────────────────────────────────┐
│ 🔍 工作                              │
├─────────────────────────────────────┤
│ 📅 2024-12-08                   [X]  │
├─────────────────────────────────────┤
│ [全部] [凌晨] [早上] [下午] [晚上]   │
└─────────────────────────────────────┘

结果：只显示 12月8日 的"工作"记录
```

### 搜索 + 日期 + 时间段
```
┌─────────────────────────────────────┐
│ 🔍 工作                              │
├─────────────────────────────────────┤
│ 📅 2024-12-08                   [X]  │
├─────────────────────────────────────┤
│ [全部] [凌晨] [早上] [下午] [晚上]   │
└─────────────────────────────────────┘

结果：只显示 12月8日下午 的"工作"记录
```

## 优势

### 1. 灵活性
- 可以只搜索
- 可以只筛选
- 可以搜索后再筛选

### 2. 直观性
- 搜索时自动清空筛选，避免混淆
- 清空按钮明显，易于操作
- 筛选条件实时生效

### 3. 效率
- 快速找到特定数据
- 支持多维度组合查询
- 减少手动翻页

## 文件修改
- `index.html` - 修改筛选逻辑、添加 useEffect、添加清空按钮

## 状态
✅ 实现完成
✅ 代码编译成功
✅ 搜索自动清空筛选
✅ 搜索后可二次筛选
✅ 日期可清空
