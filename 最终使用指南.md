# 🎯 LifeOS Timer - 最终使用指南

## 📢 重要提示

**问题已完全修复！现在只需刷新浏览器即可使用。**

---

## 🚀 快速开始（3 步）

### 第 1 步：确认服务器运行
服务器已经在运行中（进程 ID: 2），端口 8000。

如果需要重启：
```bash
./start.sh
```

### 第 2 步：刷新浏览器
**必须使用硬刷新清除缓存！**

- **Mac**: `Cmd + Shift + R`
- **Windows/Linux**: `Ctrl + Shift + R`

### 第 3 步：开始使用
打开 http://localhost:8000，点击底部导航栏的 "计时器" 标签。

---

## ✅ 预期效果

刷新后应该看到：

```
┌─────────────────────────────────────┐
│  LifeOS                             │
│  Macaron v3.9.2                     │
├─────────────────────────────────────┤
│                                      │
│  计时器                        [新建] │
│                                      │
│  📁 工作 (0)                    [▼]  │
│    暂无计时器                        │
│    [+ 添加计时器]                    │
│                                      │
│  📁 学习 (0)                    [▼]  │
│    暂无计时器                        │
│    [+ 添加计时器]                    │
│                                      │
│  📁 休息 (0)                    [▼]  │
│  📁 睡眠 (0)                    [▼]  │
│  📁 生活 (0)                    [▼]  │
│  📁 健康 (0)                    [▼]  │
│  📁 兴趣 (0)                    [▼]  │
│  📁 娱乐 (0)                    [▼]  │
│                                      │
│  [+ 新建分类组]                      │
│                                      │
└─────────────────────────────────────┘
```

---

## 🎮 功能演示

### 1️⃣ 创建秒表计时器

**步骤**：
1. 点击右上角 **"新建"** 按钮
2. 填写信息：
   - **名称**：`深度工作`
   - **图标**：点击 `⏱️`
   - **分类**：选择 `工作`
   - **模式**：选择 `秒表`（左边第一个）
3. 点击 **"创建计时器"**

**结果**：
- 在 "工作" 分类下看到新计时器
- 显示：`⏱️ 深度工作 | 秒表`

---

### 2️⃣ 创建倒计时计时器

**步骤**：
1. 点击 **"新建"**
2. 填写信息：
   - **名称**：`专注 25 分钟`
   - **图标**：点击 `⏰`
   - **分类**：选择 `学习`
   - **模式**：选择 `倒计时`（中间）
   - **时长**：输入 `25` 分钟
3. 点击 **"创建计时器"**

**结果**：
- 在 "学习" 分类下看到新计时器
- 显示：`⏰ 专注 25 分钟 | 倒计时 25 分钟`

---

### 3️⃣ 创建番茄钟计时器

**步骤**：
1. 点击 **"新建"**
2. 填写信息：
   - **名称**：`番茄工作法`
   - **图标**：点击 `🍅`
   - **分类**：选择 `工作`
   - **模式**：选择 `番茄钟`（右边）
   - **工作时长**：`25` 分钟
   - **休息时长**：`5` 分钟
   - **循环次数**：`4` 次
3. 点击 **"创建计时器"**

**结果**：
- 在 "工作" 分类下看到新计时器
- 显示：`🍅 番茄工作法 | 番茄钟 25/5 分钟`

---

### 4️⃣ 启动和控制计时器

**启动**：
1. 找到刚创建的计时器
2. 点击右侧的 **▶️ 播放按钮**
3. 弹出控制面板

**控制面板显示**：
```
┌─────────────────────────┐
│  ⏱️ 深度工作        [×]  │
│                          │
│      00:00:00            │
│      秒表模式            │
│                          │
│  [⏸ 暂停]  [× 停止]     │
└─────────────────────────┘
```

**操作**：
- **暂停**：点击 "⏸ 暂停" - 计时停止
- **继续**：点击 "▶️ 继续" - 计时恢复
- **停止**：点击 "× 停止" - 结束计时

---

### 5️⃣ 自动记录到数据源

**秒表模式特性**：
- 当秒表运行时长 **≥ 1 分钟** 时
- 点击 "停止" 后
- 自动创建事件记录到数据源
- 可以在 "数据源" 页面查看

**示例**：
```
运行秒表 5 分钟后停止
↓
自动记录：
[WORK] 2025-12-07 21:45:00 | 2025-12-07 21:50:00 | 深度工作
```

---

## 🔍 验证步骤

### ✅ 检查 1：控制台
按 `F12` 打开开发者工具，应该看到：
```
✅ Timer Backend loaded successfully
```

### ✅ 检查 2：后端加载
在控制台输入：
```javascript
window.TimerBackend
```
应该返回一个对象，包含：
```javascript
{
  TimerStorage: {...},
  CategoryStorage: {...},
  EventStorage: {...},
  TimerManager: {...},
  ...
}
```

### ✅ 检查 3：页面显示
- 看到 8 个分类组
- 可以展开/折叠分类
- 右上角有 "新建" 按钮
- 底部导航栏有 "计时器" 标签（绿色背景）

---

## 🎨 界面说明

### 分类组
- **工作** 🔵 蓝色
- **学习** 🟢 绿色
- **休息** 🔴 粉色
- **睡眠** 🟣 紫色
- **生活** 🟠 橙色
- **健康** 💚 翠绿色
- **兴趣** 🌸 玫瑰色
- **娱乐** 🟡 黄色

### 计时器模式
1. **秒表** ⏱️
   - 从 0 开始计时
   - 无时间限制
   - 停止后自动记录（≥1 分钟）

2. **倒计时** ⏰
   - 设定时长，倒数计时
   - 到达 0 时结束
   - 可设置 1-180 分钟

3. **番茄钟** 🍅
   - 工作 + 休息循环
   - 自动切换周期
   - 可设置循环次数

---

## 🐛 常见问题

### Q1: 页面还是显示 "Timer 组件加载中..."
**A**: 浏览器缓存了旧版本
**解决**：
1. 使用硬刷新：`Cmd/Ctrl + Shift + R`
2. 或者清除缓存：
   - 打开开发者工具（F12）
   - 右键点击刷新按钮
   - 选择 "清空缓存并硬性重新加载"

### Q2: 控制台报错 "Failed to load module"
**A**: 服务器未运行或端口错误
**解决**：
```bash
# 停止旧进程
lsof -ti:8000 | xargs kill -9

# 重新启动
./start.sh
```

### Q3: 创建的计时器不见了
**A**: localStorage 可能被清除
**解决**：
- 检查浏览器设置，允许 localhost 使用 localStorage
- 重新创建计时器

### Q4: 秒表停止后没有记录
**A**: 时长不足 1 分钟
**解决**：
- 秒表必须运行 ≥ 1 分钟才会自动记录
- 这是为了避免误操作产生无效记录

---

## 📊 数据流程

```
创建计时器
    ↓
保存到 localStorage
    ↓
显示在分类列表
    ↓
点击启动
    ↓
弹出控制面板
    ↓
实时计时
    ↓
点击停止
    ↓
（秒表 ≥1 分钟）创建事件
    ↓
保存到 EventStorage
    ↓
可在 "数据源" 查看
    ↓
纳入复盘分析
```

---

## 🎯 使用场景

### 场景 1：深度工作
1. 创建秒表 "深度工作"
2. 启动计时
3. 专注工作
4. 停止后自动记录工作时长

### 场景 2：学习计划
1. 创建倒计时 "学习 25 分钟"
2. 启动计时
3. 专注学习
4. 时间到达后休息

### 场景 3：番茄工作法
1. 创建番茄钟 "番茄工作法"
2. 启动计时
3. 自动切换工作/休息
4. 完成 4 个循环

---

## 🔗 与其他功能联动

### 与数据源联动
- 秒表记录自动添加到数据源
- 可以在 "数据源" 页面查看所有记录
- 支持手动编辑和分类

### 与复盘联动
- 计时器记录纳入复盘分析
- 自动计算各分类时长
- 对比理想配比

### 与日记联动
- 可以在日记中引用计时记录
- 记录当天的工作内容

---

## 📝 技术细节

### 数据存储
- **计时器配置**: `localStorage.timers`
- **分类配置**: `localStorage.categories`
- **事件记录**: `localStorage.events`

### 数据格式
```javascript
// 计时器
{
  id: "timer_1733587200000",
  name: "深度工作",
  icon: "⏱️",
  categoryId: "work",
  mode: "stopwatch",
  settings: {},
  createdAt: "2025-12-07T21:00:00.000Z",
  updatedAt: "2025-12-07T21:00:00.000Z"
}

// 事件
{
  id: "event_1733587500000",
  name: "深度工作",
  startTime: "2025-12-07T21:00:00.000Z",
  endTime: "2025-12-07T21:05:00.000Z",
  categoryId: "work",
  source: "timer",
  priority: 2,
  timerId: "timer_1733587200000",
  createdAt: "2025-12-07T21:05:00.000Z",
  updatedAt: "2025-12-07T21:05:00.000Z"
}
```

---

## 🎉 成功！

当你看到以下内容时，说明一切正常：

✅ 页面显示 8 个分类组  
✅ 可以创建新计时器  
✅ 可以启动和控制计时器  
✅ 控制台显示 "✅ Timer Backend loaded successfully"  
✅ 秒表停止后自动记录（≥1 分钟）  

---

**现在刷新浏览器，开始使用 Timer 功能吧！** 🚀

**访问地址**: http://localhost:8000  
**服务器状态**: 🟢 运行中  
**端口**: 8000  
**进程 ID**: 2  

---

**有问题？**
- 查看控制台错误信息（F12）
- 检查 `CURRENT_STATUS.md` 文件
- 查看 `FIX_APPLIED.md` 了解修复详情
