# 音效转换工具使用说明 🎵

## 工具文件
`sound-converter.html` - 独立的音效转换工具页面

## 使用步骤

### 第一步：打开工具页面
在浏览器中打开 `sound-converter.html` 文件

### 第二步：上传音效文件
1. 准备好你的音效文件（MP3、WAV等格式）
2. 点击对应的"选择文件"按钮上传：
   - 🎺 喝彩音效
   - 👏 鼓掌音效
   - 🥁 打鼓音效
3. 可以只上传一个，也可以上传全部三个

### 第三步：试听音效
- 上传成功后，对应的"试听"按钮会激活
- 点击试听按钮确认音效是否正确

### 第四步：复制生成的代码
1. 工具会自动生成包含音效数据的代码
2. 点击"📋 复制代码"按钮
3. 代码已复制到剪贴板

### 第五步：应用到主程序
1. 打开 `index.html` 文件
2. 找到这段代码（约在2480行）：
```javascript
const [customSounds, setCustomSounds] = useState(() => {
  const saved = localStorage.getItem('lifeos_pro_custom_sounds');
  return saved ? JSON.parse(saved) : { cheer: null, clap: null, drum: null };
});
```

3. 在这段代码**之前**，粘贴工具生成的 `CUSTOM_SOUNDS` 常量定义

4. 修改初始值，将 `{ cheer: null, clap: null, drum: null }` 改为 `CUSTOM_SOUNDS`

完整示例：
```javascript
// 粘贴工具生成的代码
const CUSTOM_SOUNDS = {
  cheer: "data:audio/mpeg;base64,//uQx...",
  clap: "data:audio/mpeg;base64,//uQx...",
  drum: null
};

// 修改这里
const [customSounds, setCustomSounds] = useState(() => {
  const saved = localStorage.getItem('lifeos_pro_custom_sounds');
  return saved ? JSON.parse(saved) : CUSTOM_SOUNDS; // 改这里
});
```

### 第六步：测试
1. 保存 `index.html`
2. 刷新页面
3. 进入"复盘" → "当前进度"
4. 点击音效按钮测试

## 注意事项

### 文件大小
- 建议每个音效文件小于2MB
- 文件太大会影响页面加载速度
- 如果文件过大，可以使用在线工具压缩

### 音频格式
- 推荐使用MP3格式（兼容性最好）
- 也支持WAV、OGG、M4A等格式
- 确保浏览器支持该格式

### 音效时长
- 建议2-5秒
- 太长会影响用户体验
- 太短可能听不清

### Base64编码
- 工具会将音频文件转换为Base64编码
- 这样可以直接嵌入到代码中
- 不需要额外的音频文件

## 推荐音效资源

### 免费音效网站
1. **Freesound.org**
   - 最大的免费音效库
   - 需要注册账号
   - 搜索关键词：cheer, applause, drum

2. **Zapsplat.com**
   - 高质量音效
   - 免费下载
   - 分类清晰

3. **Soundbible.com**
   - 公共领域音效
   - 无需注册
   - 直接下载

### 搜索关键词
- 喝彩：cheer, victory, celebration, crowd
- 鼓掌：applause, clapping, clap
- 打鼓：drum roll, percussion, drums

## 故障排除

**Q: 上传后没有生成代码？**
A: 检查浏览器控制台是否有错误，确保文件格式正确

**Q: 复制按钮不工作？**
A: 手动选中代码区域的文本，右键复制

**Q: 音效在主程序中不播放？**
A: 检查代码是否正确粘贴，确保没有语法错误

**Q: 文件太大怎么办？**
A: 使用在线工具压缩音频，或选择较短的音效

## 工具特点

✅ 完全离线工作，无需网络
✅ 不上传到服务器，数据安全
✅ 自动转换为Base64编码
✅ 实时预览音效
✅ 显示文件大小
✅ 一键复制代码
✅ 清晰的使用说明

---

享受你的专属音效吧！🎉
