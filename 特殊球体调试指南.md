# 特殊球体功能调试指南

## 问题现象

点击庆祝按钮（🍬 喝彩 / 🪙 鼓掌 / ⭐ 打鼓）后，没有看到特殊球体掉落。

## 调试步骤

### 1. 打开浏览器控制台

1. 访问 `http://localhost:8000`
2. 按 `F12` 或 `Cmd+Option+I` (Mac) 打开开发者工具
3. 切换到 "Console" 标签

### 2. 进入今日进度页面

1. 点击底部导航的"复盘"
2. 选择"今日进度" Tab
3. 观察控制台输出

**预期输出**:
```
✅ dropSpecialBall函数已暴露到全局
```

### 3. 点击庆祝按钮

点击任意一个庆祝按钮，观察控制台输出。

**预期输出**:
```
🍬 喝彩按钮被点击
✅ 调用window.dropSpecialBall(cheer)
🎯 dropSpecialBall被调用: cheer
🎉 创建特殊球体 - 彩虹糖: {id: "...", type: "cheer", ...}
📊 当前特殊球体数量: 1
📳 触觉反馈已触发
```

### 4. 检查可能的问题

#### 问题 A: window.dropSpecialBall 未定义

**症状**:
```
❌ window.dropSpecialBall未定义
```

**原因**: AchievementJarContainer 组件未正确渲染或 useEffect 未执行

**解决方案**:
1. 确认"今日进度"页面已完全加载
2. 检查是否有 JavaScript 错误阻止了组件渲染
3. 刷新页面重试

#### 问题 B: dropSpecialBall 被调用但没有视觉效果

**症状**:
```
🎯 dropSpecialBall被调用: cheer
🎉 创建特殊球体 - 彩虹糖: {...}
📊 当前特殊球体数量: 1
```
但是罐子里没有看到球体

**原因**: 特殊球体渲染逻辑有问题

**解决方案**:
1. 检查 `specialBalls` 状态是否正确更新
2. 在控制台输入: `console.log(window.specialBalls)` 查看状态
3. 检查 CSS 样式是否正确应用

#### 问题 C: 球体位置不对

**症状**: 球体出现在罐子外面或看不见的地方

**原因**: 坐标计算错误

**解决方案**:
1. 检查罐子的实际宽度和高度
2. 调整 `jarWidth` 和初始 `y` 坐标
3. 在控制台查看球体位置: 
   ```javascript
   console.log('Ball position:', ball.x, ball.y);
   ```

### 5. 手动测试

在控制台直接调用函数测试:

```javascript
// 测试函数是否存在
console.log('dropSpecialBall:', typeof window.dropSpecialBall);

// 手动调用
window.dropSpecialBall('cheer');
window.dropSpecialBall('clap');
window.dropSpecialBall('drum');
```

### 6. 检查特殊球体状态

```javascript
// 查看当前特殊球体数量（需要在组件内部暴露）
console.log('Special balls count:', document.querySelectorAll('.special-ball').length);
```

## 常见错误和解决方案

### 错误 1: React Hook 错误

**错误信息**:
```
Uncaught Error: Invalid hook call
```

**原因**: useCallback 或 useState 在非 React 组件中调用

**解决方案**: 确保所有 Hook 都在 React 函数组件内部

### 错误 2: 状态更新不触发重渲染

**症状**: `setSpecialBalls` 被调用但界面没有更新

**原因**: 状态引用没有改变

**解决方案**: 确保使用 `[...prev, newBall]` 创建新数组

### 错误 3: 物理模拟不工作

**症状**: 球体不下落或不弹跳

**原因**: useEffect 的依赖项错误或 setInterval 未正确清理

**解决方案**: 检查 useEffect 的依赖数组和清理函数

## 验证清单

- [ ] 服务器正在运行 (http://localhost:8000)
- [ ] 进入"复盘 → 今日进度"页面
- [ ] 控制台显示 "✅ dropSpecialBall函数已暴露到全局"
- [ ] 点击按钮后控制台显示相应日志
- [ ] 罐子中出现特殊球体
- [ ] 球体从顶部掉落
- [ ] 球体触底后弹跳
- [ ] 5秒后球体自动消失
- [ ] 手机上有震动反馈（如果支持）

## 快速修复

如果以上都不行，尝试以下快速修复:

### 方案 1: 刷新页面
```bash
# 强制刷新（清除缓存）
Cmd+Shift+R (Mac)
Ctrl+Shift+R (Windows/Linux)
```

### 方案 2: 清除浏览器缓存
1. 打开开发者工具
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

### 方案 3: 重启服务器
```bash
# 停止服务器
Ctrl+C

# 重新启动
python3 -m http.server 8000
```

## 获取帮助

如果问题仍然存在，请提供以下信息:

1. 浏览器版本和操作系统
2. 完整的控制台错误日志
3. 点击按钮后的控制台输出
4. 是否有其他 JavaScript 错误

## 成功标志

当功能正常工作时，你应该看到:

1. ✅ 控制台有完整的日志输出
2. ✅ 罐子中出现彩色球体
3. ✅ 球体有掉落动画
4. ✅ 球体会弹跳
5. ✅ 手机会震动（如果支持）
6. ✅ 5秒后球体消失

---

**最后更新**: 2025-12-11  
**版本**: 1.0
