# 日记未完成事项功能测试指南

## 测试前准备

1. 确保已经生成了今日计划（在"状态"页面打卡并生成计划）
2. 确保 `fullHistory` 中有一些今日的完成记录
3. 确保当前时间已经过了部分计划事项的时间

## 测试步骤

### 1. 基础功能测试

#### 1.1 打开日记模态框
- 点击"每日日记"页面的"写一篇"按钮
- ✅ 应该自动显示未完成事项列表
- ✅ 未完成事项应该只包含已经到来时间的事项

#### 1.2 检查未完成事项显示
- 查看未完成事项列表
- ✅ 每个事项应该显示：时间、标题
- ✅ 应该显示未完成事项的总数量
- ✅ 每个事项都有一个原因下拉选择框

#### 1.3 选择未完成原因
- 为某个未完成事项选择原因（如"太累了"）
- ✅ 下拉框应该正确显示选中的原因
- ✅ 可以为不同事项选择不同原因
- ✅ 可以不选择原因（保持"选择原因（可选）"）

#### 1.4 保存日记
- 填写日记内容
- 点击"保存"按钮
- ✅ 日记应该成功保存
- ✅ 未完成事项应该被保存到日记中

### 2. 日期切换测试

#### 2.1 切换到历史日期
- 在日记模态框中，将日期改为昨天
- ✅ 未完成事项列表应该重新计算
- ✅ 应该显示昨天的未完成事项（如果有计划的话）

#### 2.2 切换回今天
- 将日期改回今天
- ✅ 应该重新显示今天的未完成事项
- ✅ 之前选择的原因应该被清空（因为是重新计算的）

### 3. 日记列表显示测试

#### 3.1 查看保存的日记
- 关闭模态框，查看日记列表
- ✅ 应该显示未完成事项的数量（如"未完成 3 项"）
- ✅ 应该列出所有未完成事项及其时间
- ✅ 如果选择了原因，应该显示原因图标

#### 3.2 编辑日记
- 点击某篇日记的编辑按钮
- ✅ 应该正确加载未完成事项列表
- ✅ 应该保留之前选择的原因
- ✅ 可以修改原因或添加新原因

### 4. 边界情况测试

#### 4.1 没有计划的情况
- 删除今日计划（或测试没有计划的日期）
- 打开日记模态框
- ✅ 不应该显示未完成事项列表
- ✅ 应该可以正常写日记

#### 4.2 所有事项都完成的情况
- 确保 `fullHistory` 中包含了所有计划事项
- 打开日记模态框
- ✅ 未完成事项列表应该为空或不显示
- ✅ 应该可以正常写日记

#### 4.3 未到来的事项
- 在早上测试（确保有下午或晚上的计划）
- 打开日记模态框
- ✅ 未到来的事项不应该出现在未完成列表中
- ✅ 只显示已经到来时间但未完成的事项

#### 4.4 长列表滚动
- 如果有很多未完成事项（>5项）
- ✅ 列表应该可以滚动
- ✅ 最大高度应该限制在合理范围内

### 5. 数据兼容性测试

#### 5.1 查看旧日记
- 查看使用旧格式保存的日记（如果有）
- ✅ 应该正确显示"成就"或"卡点"信息
- ✅ 不应该出现错误

#### 5.2 编辑旧日记
- 编辑一篇旧格式的日记
- ✅ 应该显示当前的未完成事项（而非旧的成就/卡点）
- ✅ 保存后应该使用新格式

### 6. AI 复盘集成测试

#### 6.1 生成复盘报告
- 在"复盘"页面生成今日复盘
- ✅ AI 应该能够读取新格式的日记数据
- ✅ 复盘报告应该包含未完成事项的分析

## 预期结果

### 成功标准
- ✅ 所有未完成事项都被正确识别
- ✅ 时间过滤逻辑正确（只显示已到来的事项）
- ✅ 原因选择功能正常
- ✅ 数据保存和加载正确
- ✅ 日记列表显示正确
- ✅ 兼容旧数据格式

### 常见问题排查

#### 问题1：未完成事项列表为空
- 检查是否有今日计划
- 检查计划的日期是否正确
- 检查当前时间是否已经过了某些事项的时间

#### 问题2：某些事项应该显示但没有显示
- 检查 `fullHistory` 中是否有该事项的完成记录
- 检查匹配算法是否正确识别了事项标题
- 检查事项时间是否已经到来

#### 问题3：保存后数据丢失
- 检查浏览器控制台是否有错误
- 检查 localStorage 是否正常工作
- 检查数据结构是否正确

## 调试技巧

### 1. 查看未完成事项计算结果
在浏览器控制台执行：
```javascript
// 获取今日未完成事项
const incompleteTasks = getTodayIncompleteTasks(getTodayDate());
console.log('未完成事项:', incompleteTasks);
```

### 2. 查看今日计划
```javascript
console.log('今日计划:', todayPlan);
console.log('计划日期:', todayPlan?.date);
console.log('计划事项:', todayPlan?.blocks);
```

### 3. 查看完成记录
```javascript
console.log('完成记录:', fullHistory);
```

### 4. 查看日记数据
```javascript
console.log('日记列表:', diaryEntries);
console.log('当前日记输入:', diaryInput);
```

## 性能测试

- 测试大量未完成事项（10+）的情况
- 测试频繁切换日期的性能
- 测试长时间运行后的内存占用

## 用户体验测试

- UI 是否清晰易懂
- 操作流程是否流畅
- 错误提示是否友好
- 移动端适配是否正常
