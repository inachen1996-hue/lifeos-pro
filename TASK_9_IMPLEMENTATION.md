# Task 9: 今日计划拖拽排序功能 ✅

## 实现内容

### 1. 添加状态管理
- `planItems`: 可拖拽的计划列表
- `draggedIndex`: 当前拖拽的索引
- `hasReordered`: 是否已重新排序

### 2. 实现拖拽功能
- `handleDragStart`: 开始拖拽
- `handleDragOver`: 拖拽经过，实时更新顺序
- `handleDragEnd`: 结束拖拽
- 使用 HTML5 原生拖拽 API

### 3. 实现时间重算
- `regeneratePlan`: 从 08:00 开始重新分配时间
- 保持每个事项的原有时长
- 按新顺序连续排列

### 4. UI 优化
- 添加 Menu 图标作为拖拽手柄
- 拖拽时显示视觉反馈（半透明、放大、阴影）
- 添加"保存新顺序"按钮（渐变紫色）
- 只在重新排序后显示保存按钮

## 用户流程
1. 生成今日计划
2. 拖拽事项到新位置
3. 点击"保存新顺序"
4. 系统自动重新分配时间

## 文件修改
- `index.html`: 添加状态、函数和 UI 修改
- `PLAN_REORDER_COMPLETE.md`: 完整实现文档

## 状态
✅ 实现完成，待测试
