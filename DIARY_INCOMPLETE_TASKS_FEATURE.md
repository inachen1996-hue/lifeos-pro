# 日记未完成事项追踪功能

## 功能概述

替换了原有的"今日有成就"功能，改为智能追踪今日计划与实际完成情况的对比，自动识别未完成的事项。

## 核心功能

### 1. 自动识别未完成事项

当用户点击"写一篇"日记时，系统会自动：
- 获取今日计划的所有事项
- 从 `fullHistory` 中提取今日实际完成的事项
- 比较两者，找出未完成的事项
- **过滤掉未到来的事项**（根据当前时间判断）

### 2. 未完成原因选择

对于每个未完成的事项，用户可以选择原因（可选）：
- 😰 太难了
- 😫 太累了
- 🔋 没能量
- 😔 情绪卡点
- ⏰ 时间不够
- 🤔 忘记了
- 🤷 其他原因

### 3. 数据结构变更

#### 旧格式（已废弃）
```javascript
{
  date: '2025-12-08',
  content: '日记内容',
  mood: '平静',
  hasAchievement: true,
  achievements: '完成了某某事',
  reason: 'hard' // 或其他原因
}
```

#### 新格式
```javascript
{
  date: '2025-12-08',
  content: '日记内容',
  mood: '平静',
  incompleteTasks: [
    {
      time: '09:00',
      title: '晨间会议',
      category: 'work',
      reason: 'forgot' // 可选
    },
    {
      time: '14:00',
      title: '健身',
      category: 'health',
      reason: 'tired'
    }
  ]
}
```

## 实现细节

### 核心函数：`getTodayIncompleteTasks(date)`

```javascript
const getTodayIncompleteTasks = (date) => {
    // 1. 获取指定日期的计划
    // 2. 从 fullHistory 提取该日期的完成记录
    // 3. 比较找出未完成的事项
    // 4. 过滤掉未到来的事项（仅针对今日）
    // 5. 返回未完成事项数组
}
```

### 时间判断逻辑

- 对于今日日记：只显示已经到来时间的未完成事项
- 对于历史日记：显示全天所有未完成事项

### 匹配算法

使用 `extractDescForMatching()` 函数提取事项描述，通过模糊匹配判断是否完成：
- 去除日期、时间戳、emoji等干扰信息
- 转换为小写进行比较
- 双向包含匹配（A包含B 或 B包含A）

## UI 展示

### 日记编辑模态框

1. **日期选择**：切换日期时自动重新计算未完成事项
2. **心情选择**：保持原有功能
3. **未完成事项列表**（新增）：
   - 显示事项数量
   - 每个事项显示时间、标题
   - 下拉选择未完成原因（可选）
   - 最大高度限制，超出可滚动
4. **日记内容**：自由文本输入

### 日记列表展示

- 显示未完成事项数量
- 列出所有未完成事项及其原因
- 使用橙色主题突出显示
- 兼容旧数据格式（显示"成就"或"卡点"）

## 数据兼容性

- ✅ 完全向后兼容旧数据格式
- ✅ 旧日记仍然显示"成就"或"卡点"信息
- ✅ 新旧数据可以共存
- ✅ AI 复盘功能已更新以支持新格式

## 使用场景

1. **每日复盘**：快速了解哪些计划没有完成
2. **习惯追踪**：识别经常未完成的事项类型
3. **原因分析**：统计未完成的主要原因
4. **计划优化**：根据未完成情况调整未来计划

## 技术优势

- 🎯 **智能过滤**：自动排除未到来的事项
- 🔄 **实时更新**：切换日期立即重新计算
- 📊 **数据驱动**：基于实际记录的客观对比
- 🎨 **用户友好**：可选填写原因，不强制
- 🔧 **易于扩展**：可添加更多原因选项

## 后续优化建议

1. 添加未完成事项的统计分析
2. 支持手动标记某些事项为"已完成"
3. 提供未完成事项的趋势图表
4. AI 分析未完成原因的模式
5. 根据未完成情况智能调整计划难度
