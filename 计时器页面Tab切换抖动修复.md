# 计时器页面Tab切换抖动修复

## 🐛 问题描述
在计时器页面切换兴趣tab（分类）时，每个页面都会抖一下，影响用户体验。

## 🔍 问题分析

通过代码分析发现问题出现在以下几个方面：

### 1. 重复的动画类
```javascript
// 问题代码：每次切换都会重新应用 animate-in 类
<div className="flex animate-in h-[calc(100vh-120px)]">
```

### 2. 频繁的DOM重新渲染
- 每次切换分类时，整个右侧内容区域都会重新渲染
- `selectedCategoryData` 的计算导致组件重新挂载
- 计时器列表的完全重新生成

### 3. 样式计算抖动
- `scale-105` 和 `shadow-xl` 的动态切换
- 复杂的渐变背景重新计算
- 内联样式的频繁更新

## 🛠️ 修复方案

### 方案1：优化动画类（推荐）
移除不必要的 `animate-in` 类，使用更平滑的过渡效果。

### 方案2：使用 React.memo 优化渲染
对计时器卡片组件进行记忆化，避免不必要的重新渲染。

### 方案3：CSS优化
使用 `transform3d` 和 `will-change` 属性优化GPU加速。

## 📝 实施步骤

1. 移除主容器的 `animate-in` 类
2. 优化分类切换的过渡动画
3. 添加 CSS 优化属性
4. 测试修复效果

## 🎯 预期效果
- 分类切换时无抖动
- 保持原有的视觉效果
- 提升整体流畅度