# 习惯打卡功能实现方案

## 功能概述
在底部复盘tab中新增"习惯打卡"功能，支持用户创建习惯、设置打卡频率、关联日程事件，并通过小日历可视化展示打卡进度。

## 功能特性

### 1. 入口位置
- 位置：底部复盘tab
- Tab顺序：当前进度 | 复盘 | 习惯打卡 | 理想配比

### 2. 习惯管理
- 新增习惯：输入习惯名称
- 关联事件：选择要关联的日程事件名称（支持模糊匹配）
- 打卡频率：
  - 每天N次
  - 每周N次
  - 每月N次

### 3. 打卡统计
- 自动从历史日程数据中统计关联事件的完成次数
- 计算当前周期的打卡进度
- 显示完成率和连续打卡天数

### 4. 日历可视化
- 小日历展示（月视图）
- 打卡日期涂色显示
- 支持查看历史月份
- 点击日期查看当天详情

## 数据结构

```typescript
interface Habit {
  id: string;
  name: string;              // 习惯名称
  eventKeywords: string[];   // 关联的事件关键词（支持多个）
  frequency: {
    type: 'daily' | 'weekly' | 'monthly';
    target: number;          // 目标次数
  };
  createdAt: string;
  color: string;             // 日历显示颜色
}

interface HabitRecord {
  habitId: string;
  date: string;              // YYYY-MM-DD
  count: number;             // 当天完成次数
  events: string[];          // 匹配到的事件列表
}
```

## 实现步骤

1. ✅ 创建数据结构和存储逻辑
2. ✅ 添加习惯打卡tab
3. ✅ 实现习惯列表和新增界面
4. ✅ 实现打卡统计逻辑
5. ✅ 实现日历可视化组件
6. ✅ 集成到现有系统

## 使用流程

1. 用户点击"习惯打卡"tab
2. 点击"+"创建新习惯
3. 输入习惯名称（如"晨跑"）
4. 输入关联事件关键词（如"跑步"、"运动"）
5. 设置打卡频率（如每周3次）
6. 系统自动统计历史数据中匹配的事件
7. 在日历上显示打卡情况
8. 查看完成率和连续打卡天数

## 技术要点

- 使用localStorage存储习惯数据
- 从现有的fullHistory中提取匹配事件
- 使用React Hooks管理状态
- 马卡龙配色风格保持一致
- 响应式设计适配手机端
